sec-balance-sheet-scraper/
│
├── app/
│   ├── __init__.py
│   ├── fetch.py
│   ├── parser.py
│   ├── db.py              ← Updated for MongoDB
│   ├── batch_run.py       ← Orchestrates the full process
│
├── data/                  ← For storing temporary HTMLs (volumed in Docker)
│
├── requirements.txt       ← Python dependencies
├── Dockerfile             ← Python app image
├── docker-compose.yml     ← MongoDB + Python app services
└── README.md              ← Updated instructions